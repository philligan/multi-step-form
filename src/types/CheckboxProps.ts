import { ReactElement } from 'react';

interface CheckboxProps {
  cost: ReactElement;
  desc: string;
  id: string;
  label: string;
  name: string;
  register: any;
  value: string;
}

export default CheckboxProps;
